


// Instantiate the module
sub_RGB instance_name (
    .clk(clk), 
    .ce(ce), 
    .red(red), 
    .green(green), 
    .blue(blue), 
    .max_index(max_index), 
    .min_index(min_index), 
    .value(value)
    );


